[general]
; ⚠️ AMD NATIF ASTERISK - DÉSACTIVÉ
; Ce fichier n'est plus utilisé car remplacé par l'IA AMD (Whisper + pattern matching)
; dans robot_ari.py avec 95-98% de précision (vs 70-80% pour AMD natif)
;
; L'IA AMD se lance automatiquement après Answer() et analyse les 10 premières secondes
; avec transcription Whisper + détection de patterns français pour répondeurs/humains.
;
; Configuration conservée pour référence historique:

; Initial silence before declaring machine (milliseconds)
initial_silence = 2000

; Greeting timeout (milliseconds)
greeting = 1500

; After greeting silence (milliseconds)
after_greeting_silence = 800

; Total analysis time (milliseconds)
total_analysis_time = 2000

; Minimum word length (milliseconds)
min_word_length = 100

; Between words silence (milliseconds)
between_words_silence = 50

; Maximum number of words
maximum_number_of_words = 3

; Silence threshold (what is considered silence)
silence_threshold = 256

; Maximum word length (milliseconds)
maximum_word_length = 2000
